using System;

using SharpSCAD;
namespace Karmatach.ThreeDeePee
{
    using static Scenes;

    class Program
    {
        static void Main( string[] args )
        {
            // ButtonHelper().Save(path: "Button Helper", saveSTL: true, savePNG: true );

            // HalfSBB.Save(path: "HalfSBB", saveSTL: true, savePNG: true );

            // LedMatrix_16x32_Board.Save(path: "LED Matrix 16x32/PNG", savePNG: true );
            // LedMatrix_16x32_Kickstand.Save(path: "LED Matrix 16x32/PNG", savePNG: true );
            // LedMatrix_16x32_Single18650.Save(path: "LED Matrix 16x32/PNG", savePNG: true );
            // LedMatrix_16x32_Assembled.Save(path: "LED Matrix 16x32/PNG", savePNG: true );

            // LedMatrix_16x32_Board.Save(path: "LED Matrix 16x32/STL", saveSTL: true );
            // LedMatrix_16x32_Kickstand.Save(path: "LED Matrix 16x32/STL", saveSTL: true );
            // LedMatrix_16x32_Single18650.Save(path: "LED Matrix 16x32/STL", saveSTL: true );
            // LedMatrix_16x32_Assembled.Save(path: "LED Matrix 16x32/STL", saveSTL: true );

            // SolderTwister.Save(path: "SolderTwister", saveSTL: true, savePNG: true );

            // SwitchSandwich.Save( path: "SwitchSandwich", saveSTL: true, savePNG: true );
        }
    }
}
